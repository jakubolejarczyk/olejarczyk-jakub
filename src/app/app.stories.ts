import { Meta, StoryObj } from '@storybook/angular';
import { App } from './app';

const meta: Meta<App> = {
  title: 'App',
  component: App,
};
export default meta;
type Story = StoryObj<App>;

export const Primary: Story = {};
